import { PageShell } from '../../shared/ui/PageShell'

export function RestaurantNewPage() {
  return (
    <PageShell title="가게 추가">
      <p className="page__placeholder">카카오맵 검색 → 등록 (구현 예정)</p>
    </PageShell>
  )
}
